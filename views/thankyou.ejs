<!DOCTYPE html>
<html>
  <head>
    <title>Votre ticket</title>
  </head>
  <body>
    <h1>Votre ticket : num&eacute;ro
    <% if (ticketNumber) { %>
      <%= ticketNumber %> 
    <% } %>
    </h1>
    servi par
    <% if (ipHost) { %>
      <%= ipHost %> 
    <% } %>  
  </body>
</html>
